# Anclora Talent (editorial platform). Currently paused — see
# src/data/products.ts (status: 'en pausa'): the CTA is rendered disabled
# regardless of this value. Vite-only vars must be prefixed VITE_ and are
# read via import.meta.env (never process.env) — see src/lib/urls.ts.
# Missing or invalid (non-http/https) values fall back to the default below.
VITE_ANCLORA_TALENT_URL=https://talent.anclora.com
